// TODO: Finalize once emails are implemented

type SignUpResponse = {
  success?: string;
  error?: string;
  redirect?: string;
};

type SignInResponse = {
  error?: string;
  redirect?: string;
};
